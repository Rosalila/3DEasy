<%= form_for(shipping_type) do |f| %>
  <% if shipping_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shipping_type.errors.count, "error") %> prohibited this shipping_type from being saved:</h2>

      <ul>
      <% shipping_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :printing_hub_id %>
    <%= f.number_field :printing_hub_id %>
  </div>

  <div class="field">
    <%= f.label :doges %>
    <%= f.text_field :doges %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
